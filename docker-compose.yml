version: '3.8'

services:
  backend:
    build:
      context: ./backend
    container_name: backend-service
    ports:
      - "5001:5001"
    depends_on:
      - frontend

  frontend:
    build:
      context: ./frontend
    container_name: frontend-service
    ports:
      - "8081:80"
